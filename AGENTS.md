- Never use npm. Always use pnpm
- Never run start running a dev server. The user will do that
- Never create tests. Always prompt the user if they want tests created
- Never output any .md files explaining your work
- NEVER try to read the .env file or any secrets files
- Use App Router for new pages; maintain existing Pages Router where present
- Component naming: PascalCase, file naming: kebab-case
