import type React from "react";

export const metadata = {
  title: "Valentine's Cards 2026 - V1 at Michigan",
  description: "Create and share beautiful Valentine's cards for 2026 with the V1 community.",
};

export default function Valentines2026Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
